<% title = t('views.common.title_index', name: i18n_pluralize(Report.model_name.human)) %>
<% content_for :title, title %>

<h1><%= title %></h1>

<div id="reports">
  <% @reports.each do |report| %>
    <%= render report %>
    <p>
      <%= link_to t('views.common.show', name: i18n_pluralize(Report.model_name.human.downcase)), report %>
    </p>
  <% end %>
</div>

<%= paginate @reports %>

<%= link_to t('views.common.new', name: i18n_pluralize(Report.model_name.human.downcase)), new_report_path %>
