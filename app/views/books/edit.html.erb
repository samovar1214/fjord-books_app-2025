<% content_for :title, "Editing book" %>

<h1><%= t('books.titles.edit') %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('books.links.show'), @book %> |
  <%= link_to t('books.links.back'), books_path %>
</nav>
