<% title = t('views.common.title_edit', name: Comment.model_name.human.downcase) %>
<% content_for :title, title %>

<h1><%= title %></h1>

<%= render "form", commentable: @commentable, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), @commentable %>
</nav>
